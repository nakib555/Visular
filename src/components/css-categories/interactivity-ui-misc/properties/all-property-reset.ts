import { CSSSubCategory } from "../../../../types-css";

export const allPropertyResetGroup: CSSSubCategory = {
  name: "All Property Reset",
  properties: [
    {
      name: "all",
      values: "initial | inherit | revert | revert-layer | unset",
    },
  ],
};
